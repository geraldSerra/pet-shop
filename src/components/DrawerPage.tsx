import { useState } from "react";
import Drawer from "@mui/material/Drawer";

interface DrawerPageProps {
  openD?: boolean;
}

const DrawerPage = ({ openD }: DrawerPageProps) => {
  const [open, setOpen] = useState(false);
  return <></>;
};

export default DrawerPage;
